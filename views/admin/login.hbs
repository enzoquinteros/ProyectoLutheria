<div class="hero">
    <div class="container">
        <div class="row">
            <div class="col"  style="margin: 200px 0">
                <form action="/admin/login" method="post">
                    <div class="form-group">
                        <label  for="usuario">Usuario</label>
                        <input  type="text" class="form-control"
                                id="usuario" aria-describedby="emailHelp"
                                name="usuario">
                    </div>
                <div class="form-group">
                        <label  for="password">Password</label>
                        <input  type="password"
                                class="form-control" id="password"
                                name="password">
                </div>     
                <button type="sumbit" class="btn btn-primary">Enviar</button>
                <a href="/" class="btn btn-danger">Salir<i class="fas
                    fa-sign-out-alt"></i></a>         
                </form>
                {{#if error}}
                    <p>Usuario o contraseña incorrecto!</p>
                {{/if}}
            </div>
        </div>
    </div>
</div>